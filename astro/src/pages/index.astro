---
import { DEFAULT_PARLIAMENT_PERIOD } from 'astro:env/server';
`${DEFAULT_PARLIAMENT_PERIOD}`; // Dummy usage to avoid unused variable warning
return Astro.rewrite(`/pp/${DEFAULT_PARLIAMENT_PERIOD}`);
---
