// TODO: Duplicate of scripts/shared/model/session.ts - find a way to share this code
export enum VoteResult {
  VOTE_FOR = 'J',
  VOTE_AGAINST = 'N',
  VOTE_ABSTENTION = 'E',
  DID_NOT_VOTE = 'O',
}

// TODO: Duplicate of scripts/shared/model/session.ts - find a way to share this code
export enum VotingResult {
  PASSED = 'PASSED',
  REJECTED = 'REJECTED',
}
